 ----------
# Enviroments:
 1. React 16
 2. React Router 4
 3. Firebase 5
 ---------- 
# Run with docker-compose:
Go to directory firebase-serverless file then:

    `docker-compose build`
    `docker-compose up`
